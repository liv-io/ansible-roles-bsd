---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : sudo
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((sudo_state == 'false') or (sudo_state == 'no') or (sudo_state == 'remove') or (sudo_state == 'quiesce') or (sudo_state == 'inactive'))
  tags:
    - unix
    - sudo

- name: task include all
  include: all.yml
  when: not ((sudo_state == 'false') or (sudo_state == 'no') or (sudo_state == 'remove') or (sudo_state == 'quiesce') or (sudo_state == 'inactive'))
  tags:
    - unix
    - sudo

- name: task include group
  include: group.yml
  when: not ((sudo_state == 'false') or (sudo_state == 'no') or (sudo_state == 'remove') or (sudo_state == 'quiesce') or (sudo_state == 'inactive'))
  tags:
    - unix
    - sudo

- name: task include host
  include: host.yml
  when: not ((sudo_state == 'false') or (sudo_state == 'no') or (sudo_state == 'remove') or (sudo_state == 'quiesce') or (sudo_state == 'inactive'))
  tags:
    - unix
    - sudo

- name: task include remove
  include: remove.yml
  when: (sudo_state == 'false') or (sudo_state == 'no') or (sudo_state == 'remove')
  tags:
    - unix
    - sudo
