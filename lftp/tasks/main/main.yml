---

- name: 'task include install'
  include: install.yml
  when: not (lftp_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - lftp

- name: 'task include remove'
  include: remove.yml
  when: (lftp_state|match('false|no|remove'))
  tags:
    - main
    - lftp
