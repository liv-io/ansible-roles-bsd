---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : echoping
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((echoping_state == 'false') or (echoping_state == 'no') or (echoping_state == 'remove') or (echoping_state == 'quiesce') or (echoping_state == 'inactive'))
  tags:
    - unix
    - echoping

- name: task include remove
  include: remove.yml
  when: (echoping_state == 'false') or (echoping_state == 'no') or (echoping_state == 'remove')
  tags:
    - unix
    - echoping
