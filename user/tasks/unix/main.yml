---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : user
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include all
  include: all.yml
  when: not ((user_state == 'false') or (user_state == 'no') or (user_state == 'disable') or (user_state == 'quiesce') or (user_state == 'inactive'))
  tags:
    - unix
    - user

- name: task include group
  include: group.yml
  when: not ((user_state == 'false') or (user_state == 'no') or (user_state == 'disable') or (user_state == 'quiesce') or (user_state == 'inactive'))
  tags:
    - unix
    - user

- name: task include host
  include: host.yml
  when: not ((user_state == 'false') or (user_state == 'no') or (user_state == 'disable') or (user_state == 'quiesce') or (user_state == 'inactive'))
  tags:
    - unix
    - user
