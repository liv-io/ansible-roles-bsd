---

- name: 'task include install'
  import_tasks: install.yml
  when: not (trafshow_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - trafshow

- name: 'task include remove'
  import_tasks: remove.yml
  when: (trafshow_state is match('false|no|remove'))
  tags:
    - trafshow
