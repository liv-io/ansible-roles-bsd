---

- name: 'task include install'
  include: install.yml
  when: not (motd_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - motd

- name: 'task include remove'
  include: remove.yml
  when: (motd_state|match('false|no|remove'))
  tags:
    - main
    - motd
