---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : systeminfo
#   |____~____|
#    ___//__//   Authors: liv community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.liv.io/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((systeminfo_state == 'false') or (systeminfo_state == 'no') or (systeminfo_state == 'remove') or (systeminfo_state == 'quiesce') or (systeminfo_state == 'inactive'))
  tags:
    - unix
    - systeminfo

- name: task include remove
  include: remove.yml
  when: (systeminfo_state == 'false') or (systeminfo_state == 'no') or (systeminfo_state == 'remove')
  tags:
    - unix
    - systeminfo
