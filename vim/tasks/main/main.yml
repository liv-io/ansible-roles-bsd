---

- name: 'task include install'
  import_tasks: install.yml
  when: not (vim_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - vim

- name: 'task include remove'
  import_tasks: remove.yml
  when: (vim_state|match('false|no|remove'))
  tags:
    - main
    - vim
