---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : install.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : vim
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: include variables
  include_vars: "{{item}}"
  with_first_found:
   - "../../vars/{{ansible_os_family}}.yml"
   - "../../vars/main.yml"
  tags:
   - install
   - unix
   - vim

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: package install vim
  pkgng:
   state: 'present'
   name: "{{package_name_vim}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - packaging
   - install
   - unix
   - vim

- name: package install vim
  portage:
   state: 'installed'
   sync: "{{portage_sync}}"
   name: "{{package_name_vim}}"
  when: (ansible_os_family == 'Gentoo')
  tags:
   - packaging
   - install
   - unix
   - vim

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: template create vimrc
  template:
   src: "{{file_src_vimrc}}"
   dest: "{{file_path_vimrc}}"
   mode: "{{file_mode_vimrc}}"
   owner: "{{file_owner_vimrc}}"
   group: "{{file_group_vimrc}}"
  tags:
   - files
   - install
   - unix
   - vim
