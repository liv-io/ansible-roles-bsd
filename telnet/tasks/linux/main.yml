---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : telnet
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((telnet_state == 'false') or (telnet_state == 'no') or (telnet_state == 'remove') or (telnet_state == 'quiesce') or (telnet_state == 'inactive'))
  tags:
   - linux
   - telnet

- name: task include remove
  include: remove.yml
  when: (telnet_state == 'false') or (telnet_state == 'no') or (telnet_state == 'remove')
  tags:
   - linux
   - telnet
