---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : siege
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((siege_state == 'false') or (siege_state == 'no') or (siege_state == 'remove') or (siege_state == 'quiesce') or (siege_state == 'inactive'))
  tags:
    - unix
    - siege

- name: task include remove
  include: remove.yml
  when: (siege_state == 'false') or (siege_state == 'no') or (siege_state == 'remove')
  tags:
    - unix
    - siege
