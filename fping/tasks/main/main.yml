---

- name: 'task include install'
  include: install.yml
  when: not (fping_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - fping

- name: 'task include remove'
  include: remove.yml
  when: (fping_state|match('false|no|remove'))
  tags:
    - main
    - fping
