---

- name: 'task include install'
  include: install.yml
  when: not (ssldump_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - ssldump

- name: 'task include remove'
  include: remove.yml
  when: (ssldump_state|match('false|no|remove'))
  tags:
    - unix
    - ssldump
