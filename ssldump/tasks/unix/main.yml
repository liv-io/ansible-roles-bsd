---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : ssldump
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((ssldump_state == 'false') or (ssldump_state == 'no') or (ssldump_state == 'remove') or (ssldump_state == 'quiesce') or (ssldump_state == 'inactive'))
  tags:
    - unix
    - ssldump

- name: task include remove
  include: remove.yml
  when: (ssldump_state == 'false') or (ssldump_state == 'no') or (ssldump_state == 'remove')
  tags:
    - unix
    - ssldump
