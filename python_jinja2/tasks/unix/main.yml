---

- name: 'task include install'
  include: install.yml
  when: not (python_jinja2_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - python_jinja2

- name: 'task include remove'
  include: remove.yml
  when: (python_jinja2_state|match('false|no|remove'))
  tags:
    - unix
    - python_jinja2
