---

- name: 'task include install'
  import_tasks: install.yml
  when: not (stress_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - stress

- name: 'task include remove'
  import_tasks: remove.yml
  when: (stress_state|match('false|no|remove'))
  tags:
    - main
    - stress
