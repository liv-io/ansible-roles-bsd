---

- name: 'task include install'
  include: install.yml
  when: not (nmap_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - nmap

- name: 'task include remove'
  include: remove.yml
  when: (nmap_state|match('false|no|remove'))
  tags:
    - main
    - nmap
