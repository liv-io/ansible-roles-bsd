---

- name: 'task include install'
  include: install.yml
  when: not (zfs_stats_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - zfs_stats

- name: 'task include remove'
  include: remove.yml
  when: (zfs_stats_state|match('false|no|remove'))
  tags:
    - unix
    - zfs_stats
