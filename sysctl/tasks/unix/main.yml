---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : sysctl
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include custom
  include: custom.yml
  when: not ((sysctl_state == 'false') or (sysctl_state == 'no') or (sysctl_state == 'remove') or (sysctl_state == 'quiesce') or (sysctl_state == 'inactive'))

- name: task include default
  include: default.yml
  when: not ((sysctl_state == 'false') or (sysctl_state == 'no') or (sysctl_state == 'remove') or (sysctl_state == 'quiesce') or (sysctl_state == 'inactive'))
