---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : freebsd_rc
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include all
  include: all.yml
  when: not ((freebsd_rc_state == 'false') or (freebsd_rc_state == 'no') or (freebsd_rc_state == 'remove') or (freebsd_rc_state == 'quiesce') or (freebsd_rc_state == 'inactive'))
  tags:
   - freebsd
   - freebsd_rc

- name: task include group
  include: group.yml
  when: not ((freebsd_rc_state == 'false') or (freebsd_rc_state == 'no') or (freebsd_rc_state == 'remove') or (freebsd_rc_state == 'quiesce') or (freebsd_rc_state == 'inactive'))
  tags:
   - freebsd
   - freebsd_rc

- name: task include host
  include: host.yml
  when: not ((freebsd_rc_state == 'false') or (freebsd_rc_state == 'no') or (freebsd_rc_state == 'remove') or (freebsd_rc_state == 'quiesce') or (freebsd_rc_state == 'inactive'))
  tags:
   - freebsd
   - freebsd_rc
