---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible task file
# Role   : bsd_diff
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not (bsd_diff_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - bsd
    - bsd_diff

- name: task include remove
  include: remove.yml
  when: (bsd_diff_state|match('false|no|remove'))
  tags:
    - bsd
    - bsd_diff
