---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : ifstatus
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((ifstatus_state == 'false') or (ifstatus_state == 'no') or (ifstatus_state == 'remove') or (ifstatus_state == 'quiesce') or (ifstatus_state == 'inactive'))
  tags:
   - linux
   - ifstatus

- name: task include remove
  include: remove.yml
  when: (ifstatus_state == 'false') or (ifstatus_state == 'no') or (ifstatus_state == 'remove')
  tags:
   - linux
   - ifstatus
