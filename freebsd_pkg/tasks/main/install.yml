---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_os_family}}{{ansible_distribution_release}}.yml"
      - "vars/{{ansible_os_family}}.yml"
      - 'vars/main.yml'
      skip: 'true'
  tags:
    - install
    - main_pkg

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'template create FreeBSD.conf'
  template:
    src: "{{file_src_freebsd_conf}}"
    dest: "{{file_path_freebsd_conf}}"
    mode: "{{file_mode_freebsd_conf}}"
    owner: "{{file_owner_freebsd_conf}}"
    group: "{{file_group_freebsd_conf}}"
  tags:
    - install
    - main_pkg

- name: 'template create pkg.conf'
  template:
    src: "{{file_src_pkg_conf}}"
    dest: "{{file_path_pkg_conf}}"
    mode: "{{file_mode_pkg_conf}}"
    owner: "{{file_owner_pkg_conf}}"
    group: "{{file_group_pkg_conf}}"
  tags:
    - install
    - main_pkg
