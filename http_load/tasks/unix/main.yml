---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : http_load
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((http_load_state == 'false') or (http_load_state == 'no') or (http_load_state == 'remove') or (http_load_state == 'quiesce') or (http_load_state == 'inactive'))
  tags:
    - unix
    - http_load

- name: task include remove
  include: remove.yml
  when: (http_load_state == 'false') or (http_load_state == 'no') or (http_load_state == 'remove')
  tags:
    - unix
    - http_load
