---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : gnu_patch
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((gnu_patch_state == 'false') or (gnu_patch_state == 'no') or (gnu_patch_state == 'remove') or (gnu_patch_state == 'quiesce') or (gnu_patch_state == 'inactive'))
  tags:
   - unix
   - gnu_patch

- name: task include remove
  include: remove.yml
  when: (gnu_patch_state == 'false') or (gnu_patch_state == 'no') or (gnu_patch_state == 'remove')
  tags:
   - unix
   - gnu_patch
