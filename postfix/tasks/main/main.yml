---

- name: 'task include install'
  import_tasks: install.yml
  when: not (postfix_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - postfix

- name: 'task include enable'
  import_tasks: enable.yml
  when: (postfix_state is match('start|on|enable'))
  tags:
    - postfix

- name: 'task include disable'
  import_tasks: disable.yml
  when: (postfix_state is match('stop|off|disable'))
  tags:
    - postfix

- name: 'task include remove'
  import_tasks: remove.yml
  when: (postfix_state is match('false|no|remove'))
  tags:
    - postfix
