---

- name: 'task include install'
  include: install.yml
  when: not (socat_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - socat

- name: 'task include remove'
  include: remove.yml
  when: (socat_state|match('false|no|remove'))
  tags:
    - main
    - socat
