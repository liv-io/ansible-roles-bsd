---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : less
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((less_state == 'false') or (less_state == 'no') or (less_state == 'remove') or (less_state == 'quiesce') or (less_state == 'inactive'))
  tags:
    - unix
    - less

- name: task include remove
  include: remove.yml
  when: (less_state == 'false') or (less_state == 'no') or (less_state == 'remove')
  tags:
    - unix
    - less
