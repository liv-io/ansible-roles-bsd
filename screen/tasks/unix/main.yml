---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : screen
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((screen_state == 'false') or (screen_state == 'no') or (screen_state == 'remove') or (screen_state == 'quiesce') or (screen_state == 'inactive'))
  tags:
    - unix
    - screen

- name: task include remove
  include: remove.yml
  when: (screen_state == 'false') or (screen_state == 'no') or (screen_state == 'remove')
  tags:
    - unix
    - screen
