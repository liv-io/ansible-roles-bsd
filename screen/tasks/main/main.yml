---

- name: 'task include install'
  include: install.yml
  when: not (screen_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - screen

- name: 'task include remove'
  include: remove.yml
  when: (screen_state|match('false|no|remove'))
  tags:
    - main
    - screen
