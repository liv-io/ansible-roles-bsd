---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : ltrace
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((ltrace_state == 'false') or (ltrace_state == 'no') or (ltrace_state == 'remove') or (ltrace_state == 'quiesce') or (ltrace_state == 'inactive'))
  tags:
   - unix
   - ltrace

- name: task include remove
  include: remove.yml
  when: (ltrace_state == 'false') or (ltrace_state == 'no') or (ltrace_state == 'remove')
  tags:
   - unix
   - ltrace
