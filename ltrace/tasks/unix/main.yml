---

- name: 'task include install'
  include: install.yml
  when: not (ltrace_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - ltrace

- name: 'task include remove'
  include: remove.yml
  when: (ltrace_state|match('false|no|remove'))
  tags:
    - unix
    - ltrace
