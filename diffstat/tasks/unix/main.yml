---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : diffstat
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((diffstat_state == 'false') or (diffstat_state == 'no') or (diffstat_state == 'remove') or (diffstat_state == 'quiesce') or (diffstat_state == 'inactive'))
  tags:
    - unix
    - diffstat

- name: task include remove
  include: remove.yml
  when: (diffstat_state == 'false') or (diffstat_state == 'no') or (diffstat_state == 'remove')
  tags:
    - unix
    - diffstat
