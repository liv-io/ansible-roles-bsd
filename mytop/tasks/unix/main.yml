---

- name: 'task include install'
  include: install.yml
  when: not (mytop_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - mytop

- name: 'task include remove'
  include: remove.yml
  when: (mytop_state|match('false|no|remove'))
  tags:
    - unix
    - mytop
