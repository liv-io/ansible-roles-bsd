---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : traceroute
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((traceroute_state == 'false') or (traceroute_state == 'no') or (traceroute_state == 'remove') or (traceroute_state == 'quiesce') or (traceroute_state == 'inactive'))
  tags:
    - unix
    - traceroute

- name: task include remove
  include: remove.yml
  when: (traceroute_state == 'false') or (traceroute_state == 'no') or (traceroute_state == 'remove')
  tags:
    - unix
    - traceroute
