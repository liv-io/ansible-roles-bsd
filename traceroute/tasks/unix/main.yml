---

- name: 'task include install'
  include: install.yml
  when: not (traceroute_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - traceroute

- name: 'task include remove'
  include: remove.yml
  when: (traceroute_state|match('false|no|remove'))
  tags:
    - unix
    - traceroute
