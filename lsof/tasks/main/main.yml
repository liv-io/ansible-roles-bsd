---

- name: 'task include install'
  include: install.yml
  when: not (lsof_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - lsof

- name: 'task include remove'
  include: remove.yml
  when: (lsof_state|match('false|no|remove'))
  tags:
    - main
    - lsof
