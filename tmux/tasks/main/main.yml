---

- name: 'task include install'
  include: install.yml
  when: not (tmux_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - tmux

- name: 'task include remove'
  include: remove.yml
  when: (tmux_state|match('false|no|remove'))
  tags:
    - main
    - tmux
