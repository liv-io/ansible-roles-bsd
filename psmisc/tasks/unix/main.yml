---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : psmisc
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((psmisc_state == 'false') or (psmisc_state == 'no') or (psmisc_state == 'remove') or (psmisc_state == 'quiesce') or (psmisc_state == 'inactive'))
  tags:
   - unix
   - psmisc

- name: task include remove
  include: remove.yml
  when: (psmisc_state == 'false') or (psmisc_state == 'no') or (psmisc_state == 'remove')
  tags:
   - unix
   - psmisc
