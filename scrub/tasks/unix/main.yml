---

- name: 'task include install'
  include: install.yml
  when: not (scrub_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - scrub

- name: 'task include remove'
  include: remove.yml
  when: (scrub_state|match('false|no|remove'))
  tags:
    - unix
    - scrub
