---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : scrub
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((scrub_state == 'false') or (scrub_state == 'no') or (scrub_state == 'remove') or (scrub_state == 'quiesce') or (scrub_state == 'inactive'))
  tags:
    - unix
    - scrub

- name: task include remove
  include: remove.yml
  when: (scrub_state == 'false') or (scrub_state == 'no') or (scrub_state == 'remove')
  tags:
    - unix
    - scrub
