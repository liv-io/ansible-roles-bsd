---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : remove.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : ansible
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: include variables
  include_vars: "{{item}}"
  with_first_found:
   - "../../../vars/{{ansible_os_family}}{{ansible_distribution_major_version}}.yml"
   - "../../../vars/{{ansible_os_family}}.yml"
   - "../../../vars/main.yml"
  tags:
   - remove
   - server
   - unix
   - ansible

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: line config ansible_run-playbook
  lineinfile:
   state: 'absent'
   line: "{{file_line_ansible_update_time}} # Ansible: ansible_run-playbook"
   regexp: "# Ansible: ansible_run-playbook$"
   dest: "{{file_path_ansible_update_time}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - files
   - remove
   - server
   - unix
   - ansible

- name: file remove ansible_run-playbook.sh
  file:
   state: 'absent'
   path: "{{file_path_ansible_run_playbook_sh}}"
  tags:
   - files
   - remove
   - server
   - unix
   - ansible

- name: file remove ansible-script.dir
  file:
   state: 'absent'
   path: "{{file_path_ansible_script_dir}}"
  tags:
   - files
   - remove
   - server
   - unix
   - ansible

- name: file remove ansible-ansible.dir
  file:
   state: 'absent'
   path: "{{file_path_ansible_ansible_dir}}"
  tags:
   - files
   - remove
   - server
   - unix
   - ansible

- name: file remove ansible-log.dir
  file:
   state: 'absent'
   path: "{{file_path_ansible_log_dir}}"
  tags:
   - files
   - remove
   - server
   - unix
   - ansible
