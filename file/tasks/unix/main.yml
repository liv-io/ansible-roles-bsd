---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : file
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((file_state == 'false') or (file_state == 'no') or (file_state == 'remove') or (file_state == 'quiesce') or (file_state == 'inactive'))
  tags:
    - unix
    - file

- name: task include remove
  include: remove.yml
  when: (file_state == 'false') or (file_state == 'no') or (file_state == 'remove')
  tags:
    - unix
    - file
