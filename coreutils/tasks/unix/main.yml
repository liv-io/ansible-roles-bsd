---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : coreutils
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((coreutils_state == 'false') or (coreutils_state == 'no') or (coreutils_state == 'remove') or (coreutils_state == 'quiesce') or (coreutils_state == 'inactive'))
  tags:
    - unix
    - coreutils

- name: task include remove
  include: remove.yml
  when: (coreutils_state == 'false') or (coreutils_state == 'no') or (coreutils_state == 'remove')
  tags:
    - unix
    - coreutils
