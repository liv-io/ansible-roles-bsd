---

- name: 'task include install'
  import_tasks: install.yml
  when: not (coreutils_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - coreutils

- name: 'task include remove'
  import_tasks: remove.yml
  when: (coreutils_state|match('false|no|remove'))
  tags:
    - main
    - coreutils
