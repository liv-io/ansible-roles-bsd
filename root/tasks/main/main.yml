---

- name: 'task include install'
  import_tasks: install.yml
  when: not (root_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - root

- name: 'task include remove'
  import_tasks: remove.yml
  when: (root_state|match('false|no|remove'))
  tags:
    - main
    - root
