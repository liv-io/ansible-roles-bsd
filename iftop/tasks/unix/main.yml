---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible task file
# Role   : iftop
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not (iftop_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - iftop

- name: task include remove
  include: remove.yml
  when: (iftop_state|match('false|no|remove'))
  tags:
    - unix
    - iftop
