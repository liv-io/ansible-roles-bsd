---

- name: 'task include install'
  import_tasks: install.yml
  when: not (icmpinfo_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - icmpinfo

- name: 'task include remove'
  import_tasks: remove.yml
  when: (icmpinfo_state|match('false|no|remove'))
  tags:
    - main
    - icmpinfo
