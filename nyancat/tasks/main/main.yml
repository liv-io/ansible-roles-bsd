---

- name: 'task include install'
  include: install.yml
  when: not (nyancat_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - nyancat

- name: 'task include remove'
  include: remove.yml
  when: (nyancat_state|match('false|no|remove'))
  tags:
    - main
    - nyancat
