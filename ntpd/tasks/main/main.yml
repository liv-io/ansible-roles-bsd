---

- name: 'task include install'
  include: install.yml
  when: not (ntpd_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - ntpd

- name: 'task include enable'
  include: enable.yml
  when: (ntpd_state|match('start|on|enable'))
  tags:
    - main
    - ntpd

- name: 'task include disable'
  include: disable.yml
  when: (ntpd_state|match('stop|off|disable'))
  tags:
    - main
    - ntpd

- name: 'task include cronjob'
  include: cronjob.yml
  when: (ntpd_state|match('cron|cronjob'))
  tags:
    - main
    - ntpd

- name: 'task include remove'
  include: remove.yml
  when: (ntpd_state|match('false|no|remove'))
  tags:
    - main
    - ntpd
