---

- name: 'task include install'
  import_tasks: install.yml
  when: not (htop_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - htop

- name: 'task include remove'
  import_tasks: remove.yml
  when: (htop_state|match('false|no|remove'))
  tags:
    - main
    - htop
