---

#-------------------------------------------------------------------------------
# VARIABLES
#-------------------------------------------------------------------------------

- name: 'include variables'
  include_vars: "{{item}}"
  with_first_found:
    - files:
      - "vars/{{ansible_os_family}}{{ansible_distribution_release}}.yml"
      - "vars/{{ansible_os_family}}.yml"
      - 'vars/main.yml'
      errors: 'ignore'
  tags:
    - remove
    - denyhosts

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: 'package remove denyhosts'
  package:
    state: 'absent'
    name: "{{package_name_denyhosts}}"
  tags:
    - remove
    - denyhosts

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: 'line remove denyhosts_enable'
  lineinfile:
    state: 'absent'
    line: "{{file_line_denyhosts_enable}}=\"NO\" # Ansible: Remove denyhosts"
    regexp: "^{{file_line_denyhosts_enable}}="
    dest: "{{file_dst_denyhosts_enable}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
    - remove
    - denyhosts

- name: 'line remove syslogd_flags'
  lineinfile:
    state: 'absent'
    line: "{{file_line_syslogd_flags}}=\"-c\" # Ansible: Config denyhosts"
    regexp: "^{{file_line_syslogd_flags}}="
    dest: "{{file_dst_syslogd_flags}}"
  notify: 'service restart syslog'
  when: (ansible_os_family == 'FreeBSD')
  tags:
    - remove
    - denyhosts

- name: 'file remove allowed-hosts'
  file:
    state: 'absent'
    path: "{{file_dst_allowed_hosts}}"
  tags:
    - remove
    - denyhosts

- name: 'file remove denyhosts.conf'
  file:
    state: 'absent'
    path: "{{file_dst_denyhosts_conf}}"
  tags:
    - remove
    - denyhosts

- name: 'file remove hosts.allow'
  file:
    state: 'absent'
    path: "{{file_dst_hosts_allow}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
    - remove
    - denyhosts

- name: 'file remove hosts.block'
  file:
    state: 'absent'
    path: "{{file_dst_hosts.block}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
    - remove
    - denyhosts

- name: 'file remove data.dir'
  file:
    state: 'absent'
    path: "{{file_dst_data_dir}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
    - remove
    - denyhosts
