---

- name: 'task include install'
  include: install.yml
  when: not (minicom_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - minicom

- name: 'task include remove'
  include: remove.yml
  when: (minicom_state|match('false|no|remove'))
  tags:
    - unix
    - minicom
