---

- name: 'task include install'
  import_tasks: install.yml
  when: not (sysbench_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - sysbench

- name: 'task include remove'
  import_tasks: remove.yml
  when: (sysbench_state|match('false|no|remove'))
  tags:
    - main
    - sysbench
