---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : openbsd_rc
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include all
  include: all.yml
  when: not ((openbsd_rc_state == 'false') or (openbsd_rc_state == 'no') or (openbsd_rc_state == 'remove') or (openbsd_rc_state == 'quiesce') or (openbsd_rc_state == 'inactive'))
  tags:
    - openbsd
    - openbsd_rc

- name: task include group
  include: group.yml
  when: not ((openbsd_rc_state == 'false') or (openbsd_rc_state == 'no') or (openbsd_rc_state == 'remove') or (openbsd_rc_state == 'quiesce') or (openbsd_rc_state == 'inactive'))
  tags:
    - openbsd
    - openbsd_rc

- name: task include host
  include: host.yml
  when: not ((openbsd_rc_state == 'false') or (openbsd_rc_state == 'no') or (openbsd_rc_state == 'remove') or (openbsd_rc_state == 'quiesce') or (openbsd_rc_state == 'inactive'))
  tags:
    - openbsd
    - openbsd_rc
