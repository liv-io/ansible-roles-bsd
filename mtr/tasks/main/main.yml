---

- name: 'task include install'
  import_tasks: install.yml
  when: not (mtr_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - mtr

- name: 'task include remove'
  import_tasks: remove.yml
  when: (mtr_state|match('false|no|remove'))
  tags:
    - main
    - mtr
