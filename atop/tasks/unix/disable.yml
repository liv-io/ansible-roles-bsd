---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : disable.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : atop
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: include variables
  include_vars: "{{item}}"
  with_first_found:
   - "../../vars/{{ansible_os_family}}{{ansible_distribution_release}}.yml"
   - "../../vars/{{ansible_os_family}}.yml"
   - "../../vars/main.yml"
  tags:
   - disable
   - unix
   - atop

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: line disable atop_enable
  lineinfile:
   state: 'present'
   line: "{{file_line_atop_enable}}=\"NO\" # Ansible: Disable atop"
   regexp: "^{{file_line_atop_enable}}="
   dest: "{{file_path_atop_enable}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - files
   - disable
   - unix
   - atop

#-------------------------------------------------------------------------------
# SYSTEM
#-------------------------------------------------------------------------------

- name: service stop atop
  service:
   state: 'stopped'
   enabled: 'no'
   name: "{{service_name_atop}}"
   pattern: "{{service_pattern_atop}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - system
   - disable
   - unix
   - atop
