---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : atop
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: GNU General Public License version 2 (GPLv2)
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((atop_state == 'false') or (atop_state == 'no') or (atop_state == 'remove'))

- name: task include enable
  include: enable.yml
  when: (atop_state == 'start') or (atop_state == 'on') or (atop_state == 'enable')

- name: task include disable
  include: disable.yml
  when: (atop_state == 'stop') or (atop_state == 'off') or (atop_state == 'disable')

- name: task include remove
  include: remove.yml
  when: (atop_state == 'false') or (atop_state == 'no') or (atop_state == 'remove')
