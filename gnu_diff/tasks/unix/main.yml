---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : gnu_diff
#   |____~____|
#    ___//__//   Authors: liv community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.liv.io/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((gnu_diff_state == 'false') or (gnu_diff_state == 'no') or (gnu_diff_state == 'remove') or (gnu_diff_state == 'quiesce') or (gnu_diff_state == 'inactive'))
  tags:
    - unix
    - gnu_diff

- name: task include remove
  include: remove.yml
  when: (gnu_diff_state == 'false') or (gnu_diff_state == 'no') or (gnu_diff_state == 'remove')
  tags:
    - unix
    - gnu_diff
