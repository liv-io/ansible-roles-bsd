---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : shell
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((shell_state == 'false') or (shell_state == 'no') or (shell_state == 'remove') or (shell_state == 'quiesce') or (shell_state == 'inactive'))
  tags:
    - unix
    - shell

- name: task include remove
  include: remove.yml
  when: (shell_state == 'false') or (shell_state == 'no') or (shell_state == 'remove')
  tags:
    - unix
    - shell
