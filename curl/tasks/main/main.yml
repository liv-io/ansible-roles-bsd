---

- name: 'task include install'
  include: install.yml
  when: not (curl_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - curl

- name: 'task include remove'
  include: remove.yml
  when: (curl_state|match('false|no|remove'))
  tags:
    - main
    - curl
