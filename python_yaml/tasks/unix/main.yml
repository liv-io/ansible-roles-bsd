---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : python_yaml
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((python_yaml_state == 'false') or (python_yaml_state == 'no') or (python_yaml_state == 'remove') or (python_yaml_state == 'quiesce') or (python_yaml_state == 'inactive'))
  tags:
    - unix
    - python_yaml

- name: task include remove
  include: remove.yml
  when: (python_yaml_state == 'false') or (python_yaml_state == 'no') or (python_yaml_state == 'remove')
  tags:
    - unix
    - python_yaml
