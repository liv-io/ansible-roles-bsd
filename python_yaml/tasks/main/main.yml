---

- name: 'task include install'
  include: install.yml
  when: not (python_yaml_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - python_yaml

- name: 'task include remove'
  include: remove.yml
  when: (python_yaml_state|match('false|no|remove'))
  tags:
    - main
    - python_yaml
