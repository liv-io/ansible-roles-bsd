---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : cronjob.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : ntp
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: include variables
  include_vars: "{{item}}"
  with_first_found:
   - "../../vars/{{ansible_os_family}}.yml"
   - "../../vars/main.yml"
  tags:
   - cronjob
   - unix
   - ntp

#-------------------------------------------------------------------------------
# SYSTEM
#-------------------------------------------------------------------------------

- name: service stop ntp
  service:
   state: 'stopped'
   enabled: 'no'
   name: "{{service_name_ntp}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - system
   - cronjob
   - unix
   - ntp

- name: service stop ntp
  service:
   state: 'stopped'
   enabled: 'no'
   name: "{{service_name_ntp}}"
  when: (ansible_os_family == 'Gentoo')
  tags:
   - system
   - cronjob
   - unix
   - ntp

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: line disable ntpd_enable
  lineinfile:
   state: 'present'
   line: "{{file_line_ntpd_enable}}=\"NO\" # Ansible: Disable ntpd"
   regexp: "^{{file_line_ntpd_enable}}="
   dest: "{{file_path_ntpd_enable}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - files
   - cronjob
   - unix
   - ntp

- name: template create ntp.conf
  template:
   src: "{{file_src_ntp_conf}}"
   dest: "{{file_path_ntp_conf}}"
   mode: "{{file_mode_ntp_conf}}"
   owner: "{{file_owner_ntp_conf}}"
   group: "{{file_group_ntp_conf}}"
  tags:
   - files
   - cronjob
   - unix
   - ntp

- name: template create ntp_update-time.sh
  template:
   src: "{{file_src_ntp_update_time_sh}}"
   dest: "{{file_path_ntp_update_time_sh}}"
   mode: "{{file_mode_ntp_update_time_sh}}"
   owner: "{{file_owner_ntp_update_time_sh}}"
   group: "{{file_group_ntp_update_time_sh}}"
  tags:
   - files
   - cronjob
   - unix
   - ntp

- name: template create ntp_update-time.cron
  template:
   src: "{{file_src_ntp_update_time_cron}}"
   dest: "{{file_path_ntp_update_time_cron}}"
   mode: "{{file_mode_ntp_update_time_cron}}"
   owner: "{{file_owner_ntp_update_time_cron}}"
   group: "{{file_group_ntp_update_time_cron}}"
  when: (ansible_os_family == 'Gentoo')
  tags:
   - files
   - cronjob
   - unix
   - ntp

- name: line config ntp_update-time
  lineinfile:
   state: 'present'
   line: "{{file_line_ntp_update_time}} # Ansible: ntp_update-time"
   regexp: "# Ansible: ntp_update-time$"
   dest: "{{file_path_ntp_update_time}}"
  when: (ansible_os_family == 'FreeBSD')
  tags:
   - files
   - cronjob
   - unix
   - ntp
