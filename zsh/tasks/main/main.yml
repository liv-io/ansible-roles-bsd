---

- name: 'task include install'
  include: install.yml
  when: not (zsh_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - zsh

- name: 'task include remove'
  include: remove.yml
  when: (zsh_state|match('false|no|remove'))
  tags:
    - main
    - zsh
