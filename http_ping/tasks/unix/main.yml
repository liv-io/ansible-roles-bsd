---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : http_ping
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((http_ping_state == 'false') or (http_ping_state == 'no') or (http_ping_state == 'remove') or (http_ping_state == 'quiesce') or (http_ping_state == 'inactive'))
  tags:
    - unix
    - http_ping

- name: task include remove
  include: remove.yml
  when: (http_ping_state == 'false') or (http_ping_state == 'no') or (http_ping_state == 'remove')
  tags:
    - unix
    - http_ping
