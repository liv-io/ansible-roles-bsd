---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : tcpreplay
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((tcpreplay_state == 'false') or (tcpreplay_state == 'no') or (tcpreplay_state == 'remove') or (tcpreplay_state == 'quiesce') or (tcpreplay_state == 'inactive'))
  tags:
    - unix
    - tcpreplay

- name: task include remove
  include: remove.yml
  when: (tcpreplay_state == 'false') or (tcpreplay_state == 'no') or (tcpreplay_state == 'remove')
  tags:
    - unix
    - tcpreplay
