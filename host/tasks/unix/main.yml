---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : host
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include all
  include: all.yml
  when: not ((host_state == 'false') or (host_state == 'no') or (host_state == 'disable') or (host_state == 'quiesce') or (host_state == 'inactive'))
  tags:
    - unix
    - host

- name: task include group
  include: group.yml
  when: not ((host_state == 'false') or (host_state == 'no') or (host_state == 'disable') or (host_state == 'quiesce') or (host_state == 'inactive'))
  tags:
    - unix
    - host

- name: task include host
  include: host.yml
  when: not ((host_state == 'false') or (host_state == 'no') or (host_state == 'disable') or (host_state == 'quiesce') or (host_state == 'inactive'))
  tags:
    - unix
    - host
