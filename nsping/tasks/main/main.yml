---

- name: 'task include install'
  include: install.yml
  when: not (nsping_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - nsping

- name: 'task include remove'
  include: remove.yml
  when: (nsping_state|match('false|no|remove'))
  tags:
    - main
    - nsping
