---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible task file
# Role   : sshpass
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not (sshpass_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - sshpass

- name: task include remove
  include: remove.yml
  when: (sshpass_state|match('false|no|remove'))
  tags:
    - unix
    - sshpass
