---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : sshpass
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((sshpass_state == 'false') or (sshpass_state == 'no') or (sshpass_state == 'remove') or (sshpass_state == 'quiesce') or (sshpass_state == 'inactive'))
  tags:
    - unix
    - sshpass

- name: task include remove
  include: remove.yml
  when: (sshpass_state == 'false') or (sshpass_state == 'no') or (sshpass_state == 'remove')
  tags:
    - unix
    - sshpass
