---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible task file
# Role   : common
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not (common_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - common

- name: task include remove
  include: remove.yml
  when: (common_state|match('false|no|remove'))
  tags:
    - unix
    - common
