---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : common
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((common_state == 'false') or (common_state == 'no') or (common_state == 'remove') or (common_state == 'quiesce') or (common_state == 'inactive'))
  tags:
    - unix
    - common

- name: task include remove
  include: remove.yml
  when: (common_state == 'false') or (common_state == 'no') or (common_state == 'remove')
  tags:
    - unix
    - common
