---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : portage
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((portage_state == 'false') or (portage_state == 'no') or (portage_state == 'disable') or (portage_state == 'quiesce') or (portage_state == 'inactive'))

- name: task include custom
  include: custom.yml
  when: not ((portage_state == 'false') or (portage_state == 'no') or (portage_state == 'disable') or (portage_state == 'quiesce') or (portage_state == 'inactive'))

- name: task include default
  include: default.yml
  when: not ((portage_state == 'false') or (portage_state == 'no') or (portage_state == 'disable') or (portage_state == 'quiesce') or (portage_state == 'inactive'))
