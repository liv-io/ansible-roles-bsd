---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : wget
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((wget_state == 'false') or (wget_state == 'no') or (wget_state == 'remove') or (wget_state == 'quiesce') or (wget_state == 'inactive'))
  tags:
    - unix
    - wget

- name: task include remove
  include: remove.yml
  when: (wget_state == 'false') or (wget_state == 'no') or (wget_state == 'remove')
  tags:
    - unix
    - wget
