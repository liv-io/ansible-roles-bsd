---

- name: 'task include install'
  include: install.yml
  when: not (wget_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - wget

- name: 'task include remove'
  include: remove.yml
  when: (wget_state|match('false|no|remove'))
  tags:
    - main
    - wget
