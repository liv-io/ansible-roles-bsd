---

- name: 'task include install'
  include: install.yml
  when: not (pftop_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - pftop

- name: 'task include remove'
  include: remove.yml
  when: (pftop_state|match('false|no|remove'))
  tags:
    - main
    - pftop
