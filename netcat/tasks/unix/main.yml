---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : netcat
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((netcat_state == 'false') or (netcat_state == 'no') or (netcat_state == 'remove') or (netcat_state == 'quiesce') or (netcat_state == 'inactive'))
  tags:
    - unix
    - netcat

- name: task include remove
  include: remove.yml
  when: (netcat_state == 'false') or (netcat_state == 'no') or (netcat_state == 'remove')
  tags:
    - unix
    - netcat
