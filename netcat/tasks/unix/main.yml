---

- name: 'task include install'
  include: install.yml
  when: not (netcat_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - netcat

- name: 'task include remove'
  include: remove.yml
  when: (netcat_state|match('false|no|remove'))
  tags:
    - unix
    - netcat
