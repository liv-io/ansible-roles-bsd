---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : dsniff
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((dsniff_state == 'false') or (dsniff_state == 'no') or (dsniff_state == 'remove') or (dsniff_state == 'quiesce') or (dsniff_state == 'inactive'))
  tags:
    - unix
    - dsniff

- name: task include remove
  include: remove.yml
  when: (dsniff_state == 'false') or (dsniff_state == 'no') or (dsniff_state == 'remove')
  tags:
    - unix
    - dsniff
