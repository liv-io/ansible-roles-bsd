---

- name: 'task include install'
  import_tasks: install.yml
  when: not (cron_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - cron

- name: 'task include enable'
  import_tasks: enable.yml
  when: (cron_state|match('start|on|enable'))
  tags:
    - main
    - cron

- name: 'task include disable'
  import_tasks: disable.yml
  when: (cron_state|match('stop|off|disable'))
  tags:
    - main
    - cron

- name: 'task include remove'
  import_tasks: remove.yml
  when: (cron_state|match('false|no|remove'))
  tags:
    - main
    - cron
