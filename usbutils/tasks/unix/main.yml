---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible task file
# Role   : usbutils
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not (usbutils_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - usbutils

- name: task include remove
  include: remove.yml
  when: (usbutils_state|match('false|no|remove'))
  tags:
    - unix
    - usbutils
