---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : usbutils
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((usbutils_state == 'false') or (usbutils_state == 'no') or (usbutils_state == 'remove') or (usbutils_state == 'quiesce') or (usbutils_state == 'inactive'))
  tags:
    - unix
    - usbutils

- name: task include remove
  include: remove.yml
  when: (usbutils_state == 'false') or (usbutils_state == 'no') or (usbutils_state == 'remove')
  tags:
    - unix
    - usbutils
