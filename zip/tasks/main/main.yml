---

- name: 'task include install'
  include: install.yml
  when: not (zip_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - zip

- name: 'task include remove'
  include: remove.yml
  when: (zip_state|match('false|no|remove'))
  tags:
    - main
    - zip
