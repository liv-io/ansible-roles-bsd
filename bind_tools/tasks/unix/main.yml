---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : bind_tools
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((bind_tools_state == 'false') or (bind_tools_state == 'no') or (bind_tools_state == 'remove') or (bind_tools_state == 'quiesce') or (bind_tools_state == 'inactive'))
  tags:
   - unix
   - bind_tools

- name: task include remove
  include: remove.yml
  when: (bind_tools_state == 'false') or (bind_tools_state == 'no') or (bind_tools_state == 'remove')
  tags:
   - unix
   - bind_tools
