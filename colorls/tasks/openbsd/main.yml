---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : colorls
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((colorls_state == 'false') or (colorls_state == 'no') or (colorls_state == 'remove') or (colorls_state == 'quiesce') or (colorls_state == 'inactive'))
  tags:
    - openbsd
    - colorls

- name: task include remove
  include: remove.yml
  when: (colorls_state == 'false') or (colorls_state == 'no') or (colorls_state == 'remove')
  tags:
    - openbsd
    - colorls
