---

- name: 'task include install'
  import_tasks: install.yml
  when: not (periodic_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - periodic

- name: 'task include remove'
  import_tasks: remove.yml
  when: (periodic_state|match('false|no|remove'))
  tags:
    - main
    - periodic
