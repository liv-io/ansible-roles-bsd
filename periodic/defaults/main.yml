---
#-------------------------------------------------------------------------------
#
# Name   : main.yml
# Type   : ansible default file
# Role   : periodic
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

periodic_state: 'install'

periodic_daily_output: 'root'

periodic_daily_show_badconfig: 'false'

periodic_daily_show_info: 'true'

periodic_daily_show_success: 'true'

periodic_monthly_output: 'root'

periodic_monthly_show_badconfig: 'false'

periodic_monthly_show_info: 'true'

periodic_monthly_show_success: 'true'

periodic_periodic_conf_template: "{{ansible_os_family}}"

periodic_weekly_output: 'root'

periodic_weekly_show_badconfig: 'false'

periodic_weekly_show_info: 'true'

periodic_weekly_show_success: 'true'
