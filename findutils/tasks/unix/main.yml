---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : findutils
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((findutils_state == 'false') or (findutils_state == 'no') or (findutils_state == 'remove') or (findutils_state == 'quiesce') or (findutils_state == 'inactive'))
  tags:
    - unix
    - findutils

- name: task include remove
  include: remove.yml
  when: (findutils_state == 'false') or (findutils_state == 'no') or (findutils_state == 'remove')
  tags:
    - unix
    - findutils
