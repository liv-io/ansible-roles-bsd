#-------------------------------------------------------------------------------
#                             ANSIBLE MANAGED FILE
#-------------------------------------------------------------------------------

#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : zfs_scrub-pools.cron
#   |  _   _  |  Type   : cronjob
#   | [_] [_] |  Role   : zfs
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
# This cronjob scrubs ZFS pools on a regular basis.
#
#-------------------------------------------------------------------------------

#-------------------------------------------------------------------------------
# SHELL VARIABLES
#-------------------------------------------------------------------------------

SHELL='/bin/sh'
PATH='/sbin:/bin:/usr/sbin:/usr/bin'
MAILTO='{{file_owner_zfs_scrub_pools_sh}}'

#-------------------------------------------------------------------------------
# CRONJOB
#-------------------------------------------------------------------------------

{% if (zfs_scrub_pools_cron_minute is defined) and ((zfs_scrub_pools_cron_minute|int(20)) > 59) %}59{% elif (zfs_scrub_pools_cron_minute is defined) and ((zfs_scrub_pools_cron_minute|int(20)) < 1) %}1{% else %}{{zfs_scrub_pools_cron_minute}}{% endif %} {% if (zfs_scrub_pools_cron_hour is defined) and ((zfs_scrub_pools_cron_hour|int(2)) > 24) %}0{% elif (zfs_scrub_pools_cron_hour is defined) and ((zfs_scrub_pools_cron_hour|int(2)) < 0) %}0{% else %}{{zfs_scrub_pools_cron_hour}}{% endif %} * * {% if (zfs_scrub_pools_cron_weekday is defined) and ((zfs_scrub_pools_cron_weekday|int(6)) > 7) %}7{% elif (zfs_scrub_pools_cron_weekday is defined) and ((zfs_scrub_pools_cron_weekday|int(6)) < 1) %}0{% else %}{{zfs_scrub_pools_cron_weekday}}{% endif %} {{file_owner_zfs_scrub_pools_sh}} {{file_path_zfs_scrub_pools_sh}}
