---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : nano
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((nano_state == 'false') or (nano_state == 'no') or (nano_state == 'remove') or (nano_state == 'quiesce') or (nano_state == 'inactive'))
  tags:
    - unix
    - nano

- name: task include remove
  include: remove.yml
  when: (nano_state == 'false') or (nano_state == 'no') or (nano_state == 'remove')
  tags:
    - unix
    - nano
