---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : sdparm
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((sdparm_state == 'false') or (sdparm_state == 'no') or (sdparm_state == 'remove') or (sdparm_state == 'quiesce') or (sdparm_state == 'inactive'))
  tags:
    - unix
    - sdparm

- name: task include remove
  include: remove.yml
  when: (sdparm_state == 'false') or (sdparm_state == 'no') or (sdparm_state == 'remove')
  tags:
    - unix
    - sdparm
