---

- name: 'task include install'
  import_tasks: install.yml
  when: not (poink_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - poink

- name: 'task include remove'
  import_tasks: remove.yml
  when: (poink_state|match('false|no|remove'))
  tags:
    - main
    - poink
