---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : poink
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((poink_state == 'false') or (poink_state == 'no') or (poink_state == 'remove') or (poink_state == 'quiesce') or (poink_state == 'inactive'))
  tags:
    - openbsd
    - poink

- name: task include remove
  include: remove.yml
  when: (poink_state == 'false') or (poink_state == 'no') or (poink_state == 'remove')
  tags:
    - openbsd
    - poink
