---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : fstab
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include custom
  include: custom.yml
  when: not ((fstab_state == 'false') or (fstab_state == 'no') or (fstab_state == 'remove') or (fstab_state == 'quiesce') or (fstab_state == 'inactive'))

- name: task include default
  include: default.yml
  when: not ((fstab_state == 'false') or (fstab_state == 'no') or (fstab_state == 'remove') or (fstab_state == 'quiesce') or (fstab_state == 'inactive'))
