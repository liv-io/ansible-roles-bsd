---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : vlock
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((vlock_state == 'false') or (vlock_state == 'no') or (vlock_state == 'remove') or (vlock_state == 'quiesce') or (vlock_state == 'inactive'))
  tags:
    - unix
    - vlock

- name: task include remove
  include: remove.yml
  when: (vlock_state == 'false') or (vlock_state == 'no') or (vlock_state == 'remove')
  tags:
    - unix
    - vlock
