---

- name: 'task include install'
  include: install.yml
  when: not (vlock_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - vlock

- name: 'task include remove'
  include: remove.yml
  when: (vlock_state|match('false|no|remove'))
  tags:
    - main
    - vlock
