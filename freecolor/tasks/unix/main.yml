---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : freecolor
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((freecolor_state == 'false') or (freecolor_state == 'no') or (freecolor_state == 'remove') or (freecolor_state == 'quiesce') or (freecolor_state == 'inactive'))
  tags:
    - unix
    - freecolor

- name: task include remove
  include: remove.yml
  when: (freecolor_state == 'false') or (freecolor_state == 'no') or (freecolor_state == 'remove')
  tags:
    - unix
    - freecolor
