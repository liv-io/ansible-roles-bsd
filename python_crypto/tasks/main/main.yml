---

- name: 'task include install'
  include: install.yml
  when: not (python_crypto_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - python_crypto

- name: 'task include remove'
  include: remove.yml
  when: (python_crypto_state|match('false|no|remove'))
  tags:
    - main
    - python_crypto
