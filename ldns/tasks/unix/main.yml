---

- name: 'task include install'
  include: install.yml
  when: not (ldns_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - ldns

- name: 'task include remove'
  include: remove.yml
  when: (ldns_state|match('false|no|remove'))
  tags:
    - unix
    - ldns
