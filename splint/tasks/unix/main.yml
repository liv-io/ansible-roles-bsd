---

- name: task include install
  include: install.yml
  when: not (splint_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - splint

- name: task include remove
  include: remove.yml
  when: (splint_state|match('false|no|remove'))
  tags:
    - unix
    - splint
