---

- name: 'task include install'
  include: install.yml
  when: not (bash_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - bash

- name: 'task include remove'
  include: remove.yml
  when: (bash_state|match('false|no|remove'))
  tags:
    - main
    - bash
