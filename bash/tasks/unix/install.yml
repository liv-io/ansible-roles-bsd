---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : install.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : bash
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: GNU General Public License version 2 (GPLv2)
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: include variables
  include_vars: "{{item}}"
  with_first_found:
   - "../../vars/{{ansible_os_family}}.yml"
   - "../../vars/main.yml"

#-------------------------------------------------------------------------------
# PACKAGING
#-------------------------------------------------------------------------------

- name: package install bash
  portage:
   state: 'installed'
   sync: "{{portage_sync}}"
   name: "{{package_name_bash}}"
  when: (ansible_os_family == 'Gentoo')
  tags:
   - packaging
   - install
   - unix
   - bash

#-------------------------------------------------------------------------------
# FILES
#-------------------------------------------------------------------------------

- name: template create bash.sh
  template:
   src: "{{file_src_bash_sh}}"
   dest: "{{file_path_bash_sh}}"
   mode: "{{file_mode_bash_sh}}"
   owner: "{{file_owner_bash_sh}}"
   group: "{{file_group_bash_sh}}"
  tags:
   - files
   - install
   - unix
   - bash

- name: template create z_bash.sh
  template:
   src: "{{file_src_z_bash_sh}}"
   dest: "{{file_path_z_bash_sh}}"
   mode: "{{file_mode_z_bash_sh}}"
   owner: "{{file_owner_z_bash_sh}}"
   group: "{{file_group_z_bash_sh}}"
  tags:
   - files
   - install
   - unix
   - bash
