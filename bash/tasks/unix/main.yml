---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : bash
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((bash_state == 'false') or (bash_state == 'no') or (bash_state == 'remove') or (bash_state == 'quiesce') or (bash_state == 'inactive'))
  tags:
    - unix
    - bash

- name: task include remove
  include: remove.yml
  when: (bash_state == 'false') or (bash_state == 'no') or (bash_state == 'remove')
  tags:
    - unix
    - bash
