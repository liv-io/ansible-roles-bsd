---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : bsd_grep
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((bsd_grep_state == 'false') or (bsd_grep_state == 'no') or (bsd_grep_state == 'remove') or (bsd_grep_state == 'quiesce') or (bsd_grep_state == 'inactive'))
  tags:
    - bsd
    - bsd_grep

- name: task include remove
  include: remove.yml
  when: (bsd_grep_state == 'false') or (bsd_grep_state == 'no') or (bsd_grep_state == 'remove')
  tags:
    - bsd
    - bsd_grep
