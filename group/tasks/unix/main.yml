---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#        o
#    ____|____
#   |         |  Name   : main.yml
#   |  _   _  |  Type   : ansible task file
#   | [_] [_] |  Role   : group
#   |____~____|
#    ___//__//   Authors: robomon community
#   /|     |/    License: Simplified BSD License
#  //|_____|
#    _|| ||_     Webpage: http://www.robomon.org/
#   (__| |__)
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable'))

- name: task include custom
  include: custom.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable'))

- name: task include default
  include: default.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable'))
