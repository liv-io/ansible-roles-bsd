---

- name: 'task include install'
  include: install.yml
  when: not (group_state|match('false|no|disable|quiesce|inactive'))
  tags:
    - unix
    - group

- name: 'task include config'
  include: config.yml
  when: not (group_state|match('false|no|disable|quiesce|inactive'))
  tags:
    - unix
    - group
