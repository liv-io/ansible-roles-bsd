---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : group
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable') or (group_state == 'quiesce') or (group_state == 'inactive'))
  tags:
    - unix
    - group

- name: task include all
  include: all.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable') or (group_state == 'quiesce') or (group_state == 'inactive'))
  tags:
    - unix
    - group

- name: task include group
  include: group.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable') or (group_state == 'quiesce') or (group_state == 'inactive'))
  tags:
    - unix
    - group

- name: task include host
  include: host.yml
  when: not ((group_state == 'false') or (group_state == 'no') or (group_state == 'disable') or (group_state == 'quiesce') or (group_state == 'inactive'))
  tags:
    - unix
    - group
