---

- name: 'task include install'
  include: install.yml
  when: not (group_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - group

- name: 'task include config'
  include: config.yml
  when: not (group_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - group
