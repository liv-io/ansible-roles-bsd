---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : tcpdump
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((tcpdump_state == 'false') or (tcpdump_state == 'no') or (tcpdump_state == 'remove') or (tcpdump_state == 'quiesce') or (tcpdump_state == 'inactive'))
  tags:
    - unix
    - tcpdump

- name: task include remove
  include: remove.yml
  when: (tcpdump_state == 'false') or (tcpdump_state == 'no') or (tcpdump_state == 'remove')
  tags:
    - unix
    - tcpdump
