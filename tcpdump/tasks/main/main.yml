---

- name: 'task include install'
  include: install.yml
  when: not (tcpdump_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - tcpdump

- name: 'task include remove'
  include: remove.yml
  when: (tcpdump_state|match('false|no|remove'))
  tags:
    - main
    - tcpdump
