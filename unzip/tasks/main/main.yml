---

- name: 'task include install'
  include: install.yml
  when: not (unzip_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - unzip

- name: 'task include remove'
  include: remove.yml
  when: (unzip_state|match('false|no|remove'))
  tags:
    - main
    - unzip
