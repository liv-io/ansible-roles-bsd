---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : unzip
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((unzip_state == 'false') or (unzip_state == 'no') or (unzip_state == 'remove') or (unzip_state == 'quiesce') or (unzip_state == 'inactive'))
  tags:
    - unix
    - unzip

- name: task include remove
  include: remove.yml
  when: (unzip_state == 'false') or (unzip_state == 'no') or (unzip_state == 'remove')
  tags:
    - unix
    - unzip
