---

- name: 'task include install'
  include: install.yml
  when: not (gnu_sed_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - unix
    - gnu_sed

- name: 'task include remove'
  include: remove.yml
  when: (gnu_sed_state|match('false|no|remove'))
  tags:
    - unix
    - gnu_sed
