---
#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
#   _ _         Name   : main.yml
#  | (_)_   __  Type   : ansible task file
#  | | \ \ / /  Role   : gnu_sed
#  | | |\ V /
#  |_|_| \_/    Authors: liv community
#               License: Simplified BSD License
#
#-------------------------------------------------------------------------------
# DESCRIPTION
#-------------------------------------------------------------------------------
#
#
#
#-------------------------------------------------------------------------------

- name: task include install
  include: install.yml
  when: not ((gnu_sed_state == 'false') or (gnu_sed_state == 'no') or (gnu_sed_state == 'remove') or (gnu_sed_state == 'quiesce') or (gnu_sed_state == 'inactive'))
  tags:
    - unix
    - gnu_sed

- name: task include remove
  include: remove.yml
  when: (gnu_sed_state == 'false') or (gnu_sed_state == 'no') or (gnu_sed_state == 'remove')
  tags:
    - unix
    - gnu_sed
