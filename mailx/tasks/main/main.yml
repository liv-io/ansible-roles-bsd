---

- name: 'task include install'
  include: install.yml
  when: not (mailx_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - mailx

- name: 'task include remove'
  include: remove.yml
  when: (mailx_state|match('false|no|remove'))
  tags:
    - main
    - mailx
